@if (Tasks == null)
{
    <LoadingSpinner Enabled="true"/>
}
else
{
    <ul class="list-group">
        @foreach (var task in Tasks)
        {
            <TaskItem Task=task />
        }
    </ul>
}

@code {
    public TaskDto[] Tasks { get; set; }
}
