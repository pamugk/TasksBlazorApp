@if (Tasks == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <ul class="list-group">
        @foreach (var task in Tasks)
        {
            <TaskItem Task=@task />
        }
    </ul>
}

@code {
    public Model.Task[] Tasks { get; set; }
}
